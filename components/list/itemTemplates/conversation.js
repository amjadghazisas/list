import React from 'react';

const Conversation = (props) => {

    // This will have conversation details and again use the reusable list to have replies
    // The reusable list here will open in two modes i.e. mobile and desktop mode and therefore should use delegates
    return (
        <li>
            <h1>Conversation..</h1>                
        </li>
      );

 };

  export default Conversation;